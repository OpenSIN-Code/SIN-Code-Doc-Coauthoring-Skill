# Purpose: What this file does in one sentence.
# Docs: __main__.doc.md
# __main__.py

## What this file does
Module entry point for `python -m sin_doc_coauthoring`. Delegates to `cli.main()`.

## Usage examples
```bash
python -m sin_doc_coauthoring --help
python -m sin_doc_coauthoring start --type README --title "X"
```
