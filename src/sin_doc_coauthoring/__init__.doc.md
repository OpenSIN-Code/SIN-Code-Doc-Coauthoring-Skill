# Purpose: What this file does in one sentence.
# Docs: __init__.doc.md
# __init__.py

## What this file does
Package initialization for `sin_doc_coauthoring`, exposing the public API modules.

## Which other files import / touch it
- `mcp_server.py` — imports all core modules
- `cli.py` — imports all core modules
- All test files import from this package

## Usage examples
```python
from sin_doc_coauthoring import session, outline, drafter
```
