# Purpose: What this file does in one sentence.
# Docs: tests/test_mcp_server.doc.md
# test_mcp_server.py

## What this file does
Tests for all 11 MCP tool functions. Covers each tool in isolation + a full
end-to-end workflow test (start → gather → outline → draft → review → render
→ export → EXPORTED state).

## How many tests
~30 tests across 1 test class.
